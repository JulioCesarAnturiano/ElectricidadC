import 'dart:typed_data';

/// Placeholder para móvil - no hace nada porque usa path_provider
void downloadPdfWeb(Uint8List bytes, String fileName) {
  // No hace nada en móvil, se maneja con path_provider
}
